# Iteration 2

### Goals:
- Improve the slot machine based on improvement notes in the Iteration 1 review file as well as additional additions to the AI plan file.

### Prompt being used: 

Write the full source code for an improved browser-based slot machine game using HTML, CSS, and JavaScript, building directly on the previous iteration while maintaining a clean multi-file structure (e.g., index.html, styles.css, gameLogic.js, ui.js, rng.js) with clear separation of concerns, semantic HTML, modern CSS practices (including variables instead of hardcoded colors), and well-commented, maintainable code. This iteration must fix all prior issues: fully implement or remove the avatar system (including a working close button and no reliance on devtools), redesign the chatbox into its own scrollable container with a fixed height to prevent page stretching, expand the betting system to include multiple configurable bet sizes beyond just 10 and 100, and resolve layout problems such as the overlapping red top bar, offscreen jackpot display, lack of favicon, and excessive div usage by improving responsiveness and using proper semantic elements. Upgrade the slot machine from a 3x3 to a 3x5 layout with updated logic, remove the clock/time tracking feature entirely, and enhance visuals by adding a robot-themed animation, subtle idle animations, and more impactful short-duration full-screen win effects. Implement a functional rankings/leaderboard system that tracks stats like number of plays, total winnings, and time played so the rankings tab is no longer empty, and improve overall UI appeal while keeping it clean, readable, and accessible (including reduced motion, high contrast mode, and volume controls). Additionally, refactor the RNG system to be less exposed to client-side tampering by encapsulating logic and simulating server-side randomness where possible, reducing exploitability. The final output should include all updated files with a clear, scalable structure and demonstrate meaningful improvements in functionality, usability, visual polish, and code quality over iteration 1.

#### This prompt was generated by putting all of the improvement notes from Iteration 1 into chatGPT and asking for a paragraph format LLM query input that utilized all of the specified topics. 

### Prompt adjustments
-- No prompt adjustments 
